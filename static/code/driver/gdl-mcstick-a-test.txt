##### Stick Test ###

fn Rset()
    cmd("reset(0)") # reset my client
    reset(0) # reset myself
fend


fn Test()  
    println("Starting test...")
    Wait(1)
    dlmode(2,0)
    Wait(10)
    cmd("sel(1)")
    cmd("clear(0)")
    cmd("Texts(\"Make sure\",1,0,0,2,2)")
    cmd("Texts(\"the led is\",1,0,20,2,2)")
    cmd("Texts(\"blinking\",1,0,40,2,2)")
    cmd("show()")
fend    

