##### NRF24 Min ###
fn Rset()
    cmd("reset(0)") # reset my client
    reset(0) # reset myself
fend
 
 
fn Test()  
    println("Starting test...")
    Wait(1)
    dlmode(2,0)
    Wait(10)
 
    SetMode(0,[1,2,3,4,5],1,0,0)
 
    dim b1[] = "I am DUT"
 
    EnableTx()
    c = 1
    while(1)
 
        if (Transmit(b1,len(b1),1000))
            println("Passed, counter:", c)
            
        end
        
        c = c + 1        
        wait(1000)
    wend
fend 