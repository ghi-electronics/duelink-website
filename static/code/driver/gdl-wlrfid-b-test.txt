##### RFID #####


fn Rset()
    cmd("reset(0)") # reset my client
    reset(0) # reset myself
fend

fn Test() 
    println("Starting test...")
    Wait(1)
    dlmode(2,0)
    Wait(10)
    cmd("sel(1)")
    
    a = 0
    s = 0
   
    while 1
        a = isCard()   
        if (s != a)
            s = a            
                
            cmd("Clear(0)")
            if s = 1
                if ReadCard(b1) = 0
                    println("Card read successfully")
                    println("#:",b1)
                    cmd("TextS(\"Card read\", 1, 0, 0, 1,2)")
                    cmd("TextS(\"successfully!\", 1, 0, 20, 1,2)")
                else
                    println("Card read error!")
                    cmd("TextS(\"Card read\", 1, 0, 0, 1,2)")
                    cmd("TextS(\"error!\", 1, 0, 20, 1,2)")
                end
                                 
            end
            cmd("Show()")
            
        end
        wait(10)
    wend
fend
