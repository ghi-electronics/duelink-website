##### LED R16 Driver #####
 
fn DVer()
  return 1.1
fend
 
fn SetLed(l, v) 
    if l > 16 || l<1
        return
    end
 
    DWrite(l,v)    
fend
 
fn LedOff()
    For l=1 to 17
        SetLed(l,0)
    Next
fend
 
#########################
