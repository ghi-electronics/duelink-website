##### Load #####

fn Rset()
    cmd("reset(0)") # reset my client
    reset(0) # reset myself
fend

fn Test()
    println("Starting test...")
    Wait(1)
    dlmode(2,0)
    Wait(10)
    
    while (1)
        for i = 0 to 8
            dwrite(i+1, 1)
        next
        wait(500)
        for i = 0 to 8
            dwrite(i+1, 0)
        next
        wait(500)
    wend
fend