##### Current #####
 
fn Rset()
    cmd("reset(0)") # reset my client
    reset(0) # reset myself
fend

fn Test() 
    println("Starting test...")
    Wait(1)
    dlmode(2,0)
    Wait(10)
    
    
    while 1                 
        c = Current()
        println("current: ", c)
        cmd("clear(0)")        
        cmd(fmt("Texts(\"current: ", c, "\",1,0,0,1,2)"))            
        cmd("show()")
        
        Wait(1000)       
    wend
fend