##### Retro Sound #####

fn DVer()
    return 1.0
fend

fn Rset()
    cmd("reset(0)") # reset my client
    reset(0) # reset myself
fend 
 
fn Test()
    println("Starting test...")
    Wait(1)
    dlmode(2,0)
    Wait(10)
    
    dwrite(3,1) # max volume on channel 1
    dwrite(4,1) # max volume on channel 1

    while 1

        sweep(1, 1000, 2000, 10, 255, 1000)
        wait(500)  
        sweep(1, 2000, 1000, 255, 10, 1000)
        wait(500)  
                
    wend
 
fend 

