##### Gas #####

fn Rset()
    cmd("reset(0)") # reset my client
    reset(0) # reset myself
fend 
 
fn Test()
    println("Starting test...")
    Wait(1)
    dlmode(2,0)
    Wait(10)
    dwrite(5,1)
    c = 0
    
    while 1
        if (c < 20)
            c = c + 1
        
            println("Wait for ", c, "/20 sec")
        end
        
        if (c = 20)
    
            v = SenRead()
 
            println("v = ", v)
        end
        wait(1000)  
                         
    wend
 
fend 

