##### Oled096 Test #####

fn Rset()
    cmd("reset(0)") # reset my client
    reset(0) # reset myself
fend

fn Test()
    println("Starting test...")
    Wait(1)
    dlmode(2,0)
    Wait(10)

    _x = 0
    _d = -9
    _c = 0
    while 1 
        # Test Kit's screen
        #cmd("Clear(0)")
        #cmd("TextS(\"Downlink counter:\",1,0,0,1,2)")
        #cmd(fmt("TextS(\"", _c,"\",1,0,20,1,2)"))
        # _c =  _c+1
        #cmd("Show()") 

        # DUT screen
        Clear(1)
        TextS("DUELink", 0, 25, 5, 2, 2)
        TextS(Fmt(_x), 0, 50, 25, 2, 2)
        Circle(0, _x, 50, 5)
        _x=_x+_d
        if(_x < 0 || _x > 128)
            _d = _d * -1
        end
        Show()
      
        Wait(1)        
    wend
    
fend  