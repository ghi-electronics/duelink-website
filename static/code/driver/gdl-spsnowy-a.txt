##### Snowy Driver #####

dim b1[] = [15,24,2,4,8,9,6,1,3,5,11,7]

fn DVer()
    return 1.0
fend
 
fn SetLed(l, o) 
    if (l > 12 || l < 1) # led start from 1 to
        return 
    end
    dwrite(b1[l-1],o)   
fend


