##### Pressure #####

fn Rset()
    cmd("reset(0)") # reset my client
    reset(0) # reset myself
fend

fn Test() 
    println("Starting test...")
    Wait(1)
    dlmode(2,0)
    Wait(10)
       
    s = 0
    v = 0
    while 1
        v = VRead(2)
        
        v = v * 100
        
        v =floor(v)
        
        v = v / 100
        
        if (s != v)
            
            s = v
            cmd("Clear(0)")
            cmd(fmt("TextS(\"value: ", v, "\", 1, 0, 0, 1,2)"))
            cmd("Show()") 
            println("value: ", v)
        
        end
        
        wait(1000)
          
    wend
fend