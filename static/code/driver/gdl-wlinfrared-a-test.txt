##### IR ###

fn Rset()
    cmd("reset(0)") # reset my client
    reset(0) # reset myself
fend
 
fn Test()  
    println("Starting test...")
    Wait(1)
    dlmode(2,0)
    Wait(10)
 
    IrEn(TxPin(),RxPin())
    c = 0
    while (1)
 
        IrWrite(1)
 
        r = IrRead()
        if r != -1
            if r = 2
                c = c +1        
                wait(100)
                println("Passed, count rec: ", c)
            else
                #println("value: ", r)
            end
        end
        
        wait(900)
 
    wend
fend 