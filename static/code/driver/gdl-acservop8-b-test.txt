##### Servo P8 Driver #####

fn Rset()
    cmd("reset(0)") # reset my client
    reset(0) # reset myself
fend

fn Test()
    println("Starting test...")
    Wait(1)
    dlmode(2,0)
    Wait(10)
    cmd("sel(1)")
    cmd("Clear(0)")
    cmd("TextS(\"Starting test...\", 1, 0, 0, 1,2)")
    cmd("Show()") 

    d = 1
    s = 0
    while (1)
    
        for i = 0 to 8
            ServoSt(i + 1, s)
        next
        
        cmd("Clear(0)")
        cmd(fmt("TextS(\"Degree: ",s,"\", 1, 0, 0, 1,2)"))
        cmd("Show()") 
        
        if (s = 180)
            d = -1
        else 
            if (s = 0)
                d = 1
            end
        end
        
        s = s + d*45
        
        Wait(1000)  
        
        
    wend
fend 

####################