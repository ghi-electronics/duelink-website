# In this sample:
# Scan all switches and statled is on if any switch is ON position

while (1)
    v = 0
    for i = 0 to 10
        if Read(i+1)
            v = 1
            break
        end
    next
    
    dwrite(0,v) # set statled with v value
    
    wait(100)
wend
