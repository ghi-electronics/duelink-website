# In this sample:
# Receive a byte
# Send back (byte + 1)

SerCfg(115200, 1024) # set baudrate 115200, 1024 byte buffer

while 1
    if SerB2R() > 0
        _b = SerRd() # read data
        _b = _b + 1  # increase by 1
        SerWr(_b) # write back to sender
    end
    Wait(1)
wend
