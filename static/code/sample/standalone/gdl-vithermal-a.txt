# In this sample:
# Read and display the temperature from pixel[0] to pixel[63]

while 1
    # Check only one pixel
    println("=================")
    for _i = 0 to 64
        println("value: ", PixTemp(_i))
    next
    
    wait(100) # 10fps default
wend