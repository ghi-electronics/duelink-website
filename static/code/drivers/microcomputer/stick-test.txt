##### Stick Test ###

fn Test()  
    println("Starting test...")
    Wait(1)
    dlmode(2)
    Wait(10)
    cmd("sel(1)")
    cmd("clear(0)")
    cmd("Texts(\"Make sure\",1,0,0,1,1)")
    cmd("Texts(\"the led is\",1,0,10,1,1)")
    cmd("Texts(\"blinking\",1,0,20,1,1)")
    cmd("show()")
fend    