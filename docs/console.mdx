# Console

The DUELink online console [**console.duelink.com**](https://console.duelink.com/) is an online tool designed to help in writing scripts.

<div style={{ textAlign: 'center'}}>
![DUELink Console](pathname:///img/duelink-console.webp)
</div>

The console can be used to store/modify scripts using the [internal engine's](./engine/intro) script `Recording` mode. It also has support for `Immediate` mode.

The [Regions](./engine/regions) page has details on using the console with Regions.

---

## Recording Mode

The console helps in recording, modifying, and executing scripts.

For a quick test, connect your module to a PC and then click the `Connect` button.

<div style={{ textAlign: 'center'}}>
![Connect Button](pathname:///img/console-connect.webp)
</div>

Select one of the built-in Demo scripts, found in the top menu.

<div style={{ textAlign: 'center'}}>
![Demos](pathname:///img/console-demos.webp)
</div>

Click the `Record` button to send the script to the module's memory.


<div style={{ textAlign: 'center'}}>
![Record Button](pathname:///img/console-record.webp)
</div>

You can now `Run` the code!

<div style={{ textAlign: 'center'}}>
![Play Button](pathname:///img/console-play.webp)
</div>

:::tip
The module will automatically run a loaded script on power up.
:::

You can use the `Stop` button to terminate a running program.

<div style={{ textAlign: 'center'}}>
![Stop Button](pathname:///img/console-stop.webp)
</div>

It is possible to read back a script from a module, using the `List` button.

<div style={{ textAlign: 'center'}}>
![List Button](pathname:///img/console-list.webp)
</div>

---

## Immediate Mode

The immediate text box sends and runs the code immediately on the DUELink hardware.

<div style={{ textAlign: 'center'}}>
![DUELink immediate window](pathname:///img/immediate-window.webp)
</div>

Try `statled(200,200,50)` to blink the STAT LED 50 times.

:::tip
A very powerful feature in DUELink is when a script is recorded that contains a function, and then that function can be called from the immediate window in the console. That same function can also be called from an interface, like from USB for example.
:::

To help with troubleshooting, any returned output from a module will be directed to the `Log Panel`. For example, `Print()` function will output directly in this panel.

```py
# Run this and observe the Log Panel
Print("This is where log outputs appear")
```

The Log Panel will show the output.

<div style={{ textAlign: 'center'}}>
![DUELink console output](pathname:///img/console-output.webp)
</div>




