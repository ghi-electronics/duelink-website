# SPI

---

SPI is one of the supported interface. However, since only 2 wires are available. SPI is only supported in half-duplex, where one wire is clock, and the second is data send and receive.


:::warning
SPI is still under evaluation/development. No guarantee on its availability.
:::