---
sidebar_position: 5
title: PixoBit Rev D
description: PixoBit Rev D - High-quality DUELink module
pagination_prev: null
pagination_next: null
hide_table_of_contents: true
---

import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';
import ImageSection from '@site/src/components/ImageSection';
import OrderSection from '@site/src/components/OrderSection';

# PixoBit Rev D

<ImageSection 
  product="mcpixobit-d"
  tagline="micro:bit Form Factor with 128x64 OLED 0.96"
/>

---

<Tabs className="unique-tabs" groupid="catalog" queryString="show" defaultValue="overview"
  values={[
    {
        label: "Overview",
        value: "overview"
    },
    {
        label: "Drivers",
        value: "drivers"
    },
    {
        label: "Samples",
        value: "samples"
    }
]}>

<TabItem value="overview">

PixoBit upgrades CincoBit with a graphical display. You can now show sensor data or build video games!

<table><td width='50%'>
**Key features**

• High-quality DUELink module<br/>
• Easy integration<br/>
• Professional grade components<br/>

</td><td width='50%'>
**Resources**
 
📄[Schematics](/sch/gdl-mcpixobit-d.pdf)<br/>
🔩[3D STEP file](/3d/gdl-mcpixobit-d.step)<br/>
</td></table>

</TabItem>

<TabItem value="drivers">

See [Drivers](../../engine/drivers) page for further details.

```python
# Python driver installation
import duelink
dl = duelink.DueLink()

# Example code for PixoBit Rev D
# Initialize the module
module = dl.microcomputer.pixobit()

# Basic operations
module.initialize()
module.read()
```

</TabItem>

<TabItem value="samples">

**Samples assumes Drivers are installed.**

### Script

**Script Sample**

```basic
_x = 50
_d = 10
_w = GetW()


while 1
    Clear(0)
    Circle(1,_x,GetH()/2,10)
    
    Text(Fmt(Light()),1,50,5)
    Show()
    
    _x=_x+_d
 
    if (_x > _w || _x <0)
        _d = _d * -1    
    end
    
    if BtnA()
        Buzzer(1000,100)
    end
    
    if BtnB()
        Buzzer(500,100)
    end
    
     if (IsTouch(0) || IsTouch(1) || IsTouch(2))
        Buzzer(1500,100)
    end
    
wend
```

[See full example on GitHub](https://github.com/ghi-electronics/duelink-website/tree/main/static/code/samples)

</TabItem>

</Tabs>

---

<OrderSection
    product="DUELink PixoBit D"
    partnumber="GDL-MCPIXOBIT-D"
    price="$19.99"
/>
