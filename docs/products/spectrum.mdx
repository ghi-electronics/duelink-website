---
sidebar_position: 90
title: Spectrum
description: Spectrum - High-quality DUELink module
hide_table_of_contents: true
pagination_next: null
pagination_prev: null
---

import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';
import ImageSection from '@site/src/components/ImageSection';
import OrderSection from '@site/src/components/OrderSection';

# Spectrum

<ImageSection 
  product="sospectrum-a"
  tagline="Spectrum Analyzer"
/>

---

<Tabs className="unique-tabs" groupid="catalog" queryString="show" defaultValue="overview"
  values={[
    {
        label: "Overview",
        value: "overview"
    },
    {
        label: "Drivers",
        value: "drivers"
    },
    {
        label: "Samples",
        value: "samples"
    }
]}>

<TabItem value="overview">

This module breaks down the incoming audio into 63Hz, 160Hz, 400Hz, 1Khz, 2.5Khz, 6.25Khz, 16Khz. It then measures the audio level at these frequencies.

<table><td width='50%'>
**Key features**

• Frequency Analyzer<br/>
• 7 individual frequency filters<br/>
• 37x22mm overall dimension<br/>

</td><td width='50%'>
**Resources**
 
📄[Schematics](/sch/gdl-sospectrum-a.pdf)<br/>
🔩[3D STEP file](/3d/gdl-sospectrum-a.step)<br/>
</td></table>

</TabItem>

<TabItem value="drivers">

See [Drivers](/docs/engine/drivers) page for further details.

<details>
<summary><strong>The Code!</strong></summary>

**Spectrum Driver**

```batch
##### Spectrum Driver #####

fn DVer()
    return 1.0
fend

Alias(strobePin=5, resetPin=3)
 
Dim a1[7]
 
fn Init() 
    DWrite(strobePin,1)
    DWrite(resetPin,1)
    Wait(1)
 
    DWrite(strobePin,0)
    Wait(1)
 
    DWrite(resetPin,1)
    Wait(1)
 
    DWrite(strobePin,1)
    Wait(1)
 
    DWrite(strobePin,0)
    Wait(1)
 
    DWrite(resetPin,0)
    Wait(1)
fend
 
fn UpdBnd() 
    for i = 0 to 7
        a1[i] = VRead(4)/3.3*100
 
        _t = TickUs()
 
        DWrite(strobePin,1)
        while (TickUs() - _t < 50) #Wait 50us
        wend
 
        DWrite(strobePin,0)
        while (TickUs() - _t < 100) #Wait 50us
        wend
    next
fend
 
Init()

####################
```

</details>

[See full driver on GitHub](https://github.com/ghi-electronics/duelink-website/tree/main/static/code/drivers)

</TabItem>

<TabItem value="samples">

### Python

```python
from DUELink.DUELinkController import DUELinkController
import time
availablePort = DUELinkController.GetConnectionPort()
duelink = DUELinkController(availablePort)
```

### JavaScript

```javascript
const { SerialUSB } = require("dlserialusb");
const { DUELinkController } = require("duelink");
```

### Script

```batch
while (1)
    UpdBnd()
    PrintLn("63Hz: ", a1[0])
    wait(1)
wend

```

[See full examples on GitHub](https://github.com/ghi-electronics/duelink-website/tree/main/static/code/samples)

</TabItem>

</Tabs>

---

<OrderSection
    product="DUELink Spectrum"
    partnumber="GDL-SOSPECTRUM-A"
    price="$0.00"
/>
