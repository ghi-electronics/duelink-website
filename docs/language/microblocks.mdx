# MicroBlocks

MicroBlocks is a blocks language (like Scratch) for programming DUELink boards. Drag blocks in a browser — the board reacts immediately. Click a block to try it, build scripts as you go, no compile-and-wait step.

<div style={{ textAlign: 'center'}}>
![MicroBlocks](pathname:///img/microblocks.webp)
</div>

<div style={{ textAlign: 'center'}}>
![MicroBlocks sample program](pathname:///img/microblocks-sample.webp)
</div>

---

## Quick start: what you need

**Have [CincoBit](../products/mccincobit-e) or [PixoBit](../products/mcpixobit-d)?** They ship with MicroBlocks firmware already loaded — skip ahead to [Open the editor](#open-the-editor) below.

**Have any other DUELink board?** Load MicroBlocks firmware once at [loader.duelink.com/microblocks](https://loader.duelink.com/microblocks). One-click flow; see [Firmware updates](../loader) if you hit a snag.

**Don't have a board yet?** The [Classroom CincoBit Kit](../products/ktcincobit-e) is the fastest classroom start. For a single board, [CincoBit](../products/mccincobit-e) or [PixoBit](../products/mcpixobit-d) covers most lessons.

---

## Classroom curriculum

**Teaching with CincoBit or PixoBit?** DUELink includes a complete **middle-school CS curriculum** — lesson plans and MicroBlocks projects, ready to run in the classroom.

<div style={{ textAlign: 'center'}}>
<Button
  style={{ color:'white', margin: "10px" }}
  label="Open CincoBit Curriculum →"
  link="../tutorial/cincobit"
/>
</div>

<div style={{ textAlign: 'center'}}>
![Classroom kit](pathname:///img/catalog/ktcincobit-e-1.webp)
</div>

See also [For Educators](../educators) for classroom kits and the typical teaching path.

---

## Two ways to use it

**1. Run MicroBlocks on the board** — load MicroBlocks firmware on a DUELink board like [CincoBit](../products/mccincobit-e) or [PixoBit](../products/mcpixobit-d). Your blocks run directly on the module. Follow the **[CincoBit curriculum](../tutorial/cincobit)** for structured classroom lessons.

**2. Control other modules from MicroBlocks** — MicroBlocks runs on a host board (a [micro:bit](../hw/microbit), a CincoBit, etc.) and sends plain-text **commands** to chained DUELink modules — a light sensor, RGB LEDs, a display, and more. See [Chain modules](../chain).

You can combine both: MicroBlocks on a board like CincoBit, with more modules chained on the Downlink port.

:::tip
Product pages list example projects under **Commands → Samples** (host-side) and **On-Module Code** (runs on the board).
:::

---

## Get started on the board

Educational boards like CincoBit and PixoBit ship with MicroBlocks pre-loaded. For other modules, use [loader.duelink.com/microblocks](https://loader.duelink.com/microblocks).

:::tip
You only need to install or update MicroBlocks firmware when switching from DUELink firmware, or when updating to a newer version.
:::

### Open the editor

Run MicroBlocks in **Chrome** or **Edge** at [microblocks.fun](https://microblocks.fun) — click **Run** in the top menu, or go directly to [microblocks.fun/run](https://microblocks.fun/run).

<div style={{ textAlign: 'center'}}>
![MicroBlocks in the browser](pathname:///img/microblocks-browser-run.webp)
</div>

Or download the desktop app for Windows, macOS, or Linux from the [MicroBlocks download page](https://microblocks.fun/download).

<div style={{ textAlign: 'center'}}>
![MicroBlocks download](pathname:///img/microblocks-download.webp)
</div>

### Connect your board

Click **Connect** in the editor:

<div style={{ textAlign: 'center'}}>
![Connect button](pathname:///img/microblocks-connect-1.webp)
</div>

Choose **Connect (USB)**:

<div style={{ textAlign: 'center'}}>
![Connect USB menu](pathname:///img/microblocks-connect-2.webp)
</div>

Select **DUELink MicroBlocks**, then click **Connect**:

<div style={{ textAlign: 'center'}}>
![Select DUELink MicroBlocks board](pathname:///img/microblocks-connect-3.webp)
</div>

If the board does not appear, check the USB cable and press the board's reset button.

When connected, the button turns green and the board name appears beside it:

<div style={{ textAlign: 'center'}}>
![Connected board](pathname:///img/microblocks-connect-4.webp)
</div>

### First project

Click the file icon and choose **New**:

<div style={{ textAlign: 'center'}}>
![New project](pathname:///img/microblocks-new-project.webp)
</div>

MicroBlocks loads libraries for your board:

<div style={{ textAlign: 'center'}}>
![Default libraries](pathname:///img/microblocks-default-libraries.webp)
</div>

Click the **set user LED** block in the **Output** category to verify everything works:

<div style={{ textAlign: 'center'}}>
![Set user LED block](pathname:///img/microblocks-setuserled.webp)
</div>

On most boards this turns on the green STAT LED. On CincoBit (no STAT LED), it lights the center pixel on the 5×5 matrix.

### Live coding

MicroBlocks is **live** — click any block or script to run it immediately:

<div style={{ textAlign: 'center'}}>
![Instant block execution](pathname:///img/microblocks-instant.gif)
</div>

Drag blocks into the scripting area to build programs. Code is saved in flash and keeps running when you unplug USB.

Scripts that start with **when started** run automatically on power-up:

<div style={{ textAlign: 'center'}}>
![When started block](pathname:///img/microblocks-whenstarted.webp)
</div>

Up to ten scripts can run at the same time.

### Example projects

CincoBit — button toggles smile / frown on the LED matrix:

<div style={{ textAlign: 'center'}}>
![CincoBit demo](pathname:///img/mb-demo-cinco.webp)
</div>

Clipit — same idea using the **A** touch pad:

<div style={{ textAlign: 'center'}}>
![Clipit demo](pathname:///img/mb-demo-clipit.webp)
</div>

<div style={{ textAlign: 'center'}}>
![Smile and frown animation](pathname:///img/microblocks-smile-frown.gif)
</div>

DueSTEM — light level as a bar graph, temperature on screen:

<div style={{ textAlign: 'center'}}>
![DueSTEM demo](pathname:///img/mb-demo-duestem.webp)
</div>

Click **Add Library** in the editor for nearly 200 extension libraries beyond the defaults.

### Pin numbers

On **CincoBit** and **PixoBit**, MicroBlocks uses the same edge-connector pin numbers as the [micro:bit](https://tech.microbit.org/hardware/edgeconnector/). Pins 0, 1, and 2 are the alligator-clip holes on the left side.

On CincoBit: pin 9 = light sensor, pin 21 = buzzer.

On PixoBit: pin 21 = buzzer, pin 22 = display reset, pin 23 = light sensor.

On all other DUELink boards, MicroBlocks uses standard DUELink pin numbers. Pins 0–9 and 17 support analog input. Pin numbers are marked on the board or in its schematic.

---

## Control chained modules

MicroBlocks on one board can command other DUELink modules over the chain. The host runs MicroBlocks; downstream modules keep DUELink firmware and respond to text commands — the same commands listed on each module's **Commands** tab.

**micro:bit → LED RGB3** — MicroBlocks on the micro:bit, [LinkBit](../products/adlinkbit-a) adapter for the JST Downlink port:

<div style={{ textAlign: 'center'}}>
![micro:bit with LinkBit](pathname:///img/microbit-linkbit.webp)
</div>

**PixoBit → chained modules** — MicroBlocks on PixoBit controlling a Downlink chain:

<div style={{ textAlign: 'center'}}>
![PixoBit daisylinked](pathname:///img/pixobit-daisylinked.webp)
</div>

<div style={{ textAlign: 'center'}}>
![Chained LEDs under MicroBlocks control](pathname:///img/microblocks-daisylinked.gif)
</div>

### Wiring

Connect the host board's **Downlink (D)** port to the first module's **Uplink (U)** port:

<div style={{ textAlign: 'center'}}>
![CincoBit Downlink port](pathname:///img/cincobit-downlink.webp)
</div>

<div style={{ textAlign: 'center'}}>
![RGB3 Uplink port](pathname:///img/rgb3-uplink.webp)
</div>

Chain additional modules: each module's **D** connects to the next module's **U**.

<div style={{ textAlign: 'center'}}>
![Uplink and Downlink](pathname:///img/mb-uplink-downlink.webp)
</div>

Modules can mount on a [Holey Board](../products/axholey).

For a non-DUELink host (e.g. micro:bit), use [LinkBit](../products/adlinkbit-a) or wire I2C/UART through a [Breakout](../products/adbreakout-a). Full details: [Chain modules](../chain).

:::tip
Always connect **D → U**. A **D–D** or **U–U** link anywhere in the chain will not work.
:::

Disconnect USB/power before plugging or unplugging modules. DUELink assigns addresses on power-up, so power-cycle after wiring changes.

### Add the Daisylink library

In the MicroBlocks editor, click **Add Library** → **Kits and Boards** → **DUELink Daisylink**:

<div style={{ textAlign: 'center'}}>
![Add library button](pathname:///img/add-library.webp)
</div>

<div style={{ textAlign: 'center'}}>
![DUELink Daisylink library](pathname:///img/microblocks-lib-daisylink.webp)
</div>

Click the **connected?** block to confirm the downstream module is detected:

<div style={{ textAlign: 'center'}}>
![Connected test](pathname:///img/connected-test.webp)
</div>

:::tip
Modules cannot be hot-plugged. If **connected?** fails, check wiring and power-cycle the whole chain.
:::

The first module in the chain is address **1** (selected automatically on power-up). Use **select device** when you need address 2, 3, and so on.

### Using module commands

Each chained module exposes commands through its [driver](../engine/drivers) — see the **Commands** tab on its product page. For [LED RGB3](../products/ldledrgb3-a), `SetLed(i, c)` sets LED index `i` (0–2) to color `c` (hex RRGGBB, e.g. `FF0000` for red).

The **DUELink Daisylink** library includes blocks to call these commands. Example helper for RGB3:

<div style={{ textAlign: 'center'}}>
![RGB3 driver block](pathname:///img/microblocks-rgb3-driver.png)
</div>

:::tip
Check **Commands → Samples → MicroBlocks** on a product page for a ready-made project you can open in MicroBlocks.
:::

Press button **A** to cycle RGB3 through red, green, and blue:

<div style={{ textAlign: 'center'}}>
![Blink RGB3 script](pathname:///img/blink-rgb3.png)
</div>

<div style={{ textAlign: 'center'}}>
![RGB3 animation](pathname:///img/microblocks-daisylinked.gif)
</div>

---

## Going further

### MicroBlocks on any module

You can load MicroBlocks firmware onto any DUELink module, not just classroom boards. [LED RGB3](../products/ldledrgb3-a) is a common example — use a [USB Hook](../products/adusbhook-a) since it has no USB port:

<div style={{ textAlign: 'center'}}>
![USB Hook with RGB3](pathname:///img/usbhook-rgb3.webp)
</div>

Put the module in DFU mode via the [Loader](../loader) **Erase All** flow, or short the BOOT0 pads while powering up:

<div style={{ textAlign: 'center'}}>
![Loader pads](pathname:///img/module-loader-pads.webp)
</div>

Once MicroBlocks firmware is loaded, the module no longer accepts DUELink text commands — you control hardware entirely through MicroBlocks blocks. Simple modules (LEDs, buttons, sensors) are straightforward; complex ones (displays, SD card, USB host) are easier to **chain** so the module keeps its DUELink firmware and built-in command support.

Example: run MicroBlocks on [CincoBit](../products/mccincobit-e) and chain an [SD Card](../products/stsdcard-b) module instead of loading MicroBlocks on the SD module itself:

<div style={{ textAlign: 'center'}}>
![CincoBit with SD Card module](pathname:///img/cincobit-sdcard.webp)
</div>

### Linux

Linux may need dialout group membership for USB serial access. See the **Linux** tab on [MicroBlocks Get Started](https://microblocks.fun/get-started).

<details>
<summary><strong>Example Linux setup script</strong></summary>

```bash
#!/bin/bash

echo "Adding user to dialout group for serial port access..."
sudo usermod -aG dialout $USER

if id -nG "$USER" | grep -qw "dialout"; then
    echo "User $USER added to dialout group successfully."
else
    echo "Failed to add user to dialout group."
    exit 1
fi

echo "Creating udev rule for STM32 DFU mode..."
echo 'SUBSYSTEM=="usb", ATTR{idVendor}=="0483", ATTR{idProduct}=="df11", MODE="0666"' | sudo tee /etc/udev/rules.d/99-stm32-dfu.rules > /dev/null
sudo udevadm control --reload-rules

echo "Log out and back in (or reboot) for changes to take effect."
```

Save as a `.sh` file, `chmod +x`, run once, then reboot.

</details>

### Resources

- [MicroBlocks website](https://microblocks.fun)
- [User guide](https://wiki.microblocks.fun/en/ide)
- [Blocks reference](https://wiki.microblocks.fun/en/reference_manual)
- [Libraries reference](https://wiki.microblocks.fun/en/libraries)
- [Learning guides](https://learn.microblocks.fun)
