---
hide_table_of_contents: true
---

import Tabs from "@theme/Tabs";
import TabItem from "@theme/TabItem";

# Get Started in Minutes!

Select your desired language and plug in your device to **blink its STAT LED!**

<Tabs groupid="language"
  values={[
    {label: 'Python', value: 'py'},
    {label: 'JavaScript', value: 'js'},
    {label: '.NET', value: 'dotnet'},
    {label: 'Arduino', value: 'arduino'},
    {label: 'MicroPython', value: 'mpy'},
    {label: 'Script', value: 'script'},
    {label: 'MicroBlocks', value: 'microblocks'},
    
]}>
<TabItem value="py">
<details>
  <summary>Connect to a PC (click for image)</summary>
<div style={{ textAlign: 'center'}}>
![DUELink Connection](pathname:///img/duelink-connection.webp)
</div>
</details>

```py
from DUELink.DUELinkController import DUELinkController

availablePort = DUELinkController.GetConnectionPort()
duelink = DUELinkController(availablePort)
# Blink the STAT LED  (on for 200ms, off for 800ms, 20 times)
duelink.System.StatLed(200,800,20)
```
You need `pip install DUELink`. Full details are on the [Python](./language/python) page.

</TabItem>
<TabItem value="js">
<details>
  <summary>Connect to a PC (click for image)</summary>
<div style={{ textAlign: 'center'}}>
![DUELink Connection](pathname:///img/duelink-connection.webp)
</div>
</details>
```js
import pkg_serialusb from 'dlserialusb';
const {SerialUSB} = pkg_serialusb

import pkg_duelink from 'duelink';
const {DUELinkController} = pkg_duelink

let duelink = new DUELinkController(new SerialUSB());
await duelink.Connect();

async function Blinky() {
  // Blink the STAT LED (on for 200ms, off for 800ms, 20 times)
  await duelink.System.StatLed(200, 800, 20);
}

Blinky();
```
You need `npm install duelink` and `npm install dlserialusb`. Full details are on the [JavaScript](./language/javascript) page.
</TabItem>
<TabItem value="arduino">
<details>
  <summary>Connect to Arduino UNO R4 (click for image)</summary>
<div style={{ textAlign: 'center'}}>
![Arduino UNO 4 Daisylinked](pathname:///img/arduino-connection.webp)
</div>
</details>
```cpp
#include <DUELink.h>

TwoWireTransport transport(Wire1);
DUELink duelink(transport);

void setup() {
  duelink.Connect();
  // Blink the STAT LED (on for 200ms, off for 800ms, 20 times)
  duelink.System.StatLed(200, 200, 10);
}

void loop(){
}

```
You need `DUELink` and `STM32 MCU based boards` packages. Full details are on the [Arduino](./hw/arduino) page.

</TabItem>
<TabItem value="dotnet">
<details>
  <summary>Connect to a PC (click for image)</summary>
<div style={{ textAlign: 'center'}}>
![DUELink Connection](pathname:///img/duelink-connection.webp)
</div>
</details>

```cs
using GHIElectronics.DUELink;
Console.WriteLine("Hello DUELink!");
var availablePort = DUELinkController.GetConnectionPort();
var duelink = new DUELinkController(availablePort);

// Blink the STAT LED (on for 200ms, off for 800ms, 20 times)
duelink.System.Statled(200, 800, 20);
Console.WriteLine("Bye DUELink!");
```
You need `GHIElectronics.DUELink` library from `nuget.org`. Full details are on the [.NET](./language/dotnet) page.

</TabItem>

<TabItem value="script">

DUELink modules have a built in scripting engine, allowing them to run standalone. You can load and modify scripts using [console.duelink.com](https://console.duelink.com/).
```py
# Blink the STAT LED (on for 200ms, off for 800ms, 20 times)
Statled(200, 800, 20)
# Blink the STAT LED, but on a tethered module this time
Cmd("Statled(200, 800, 20)")
```
Learn about the [scripting language](./engine/scripting) and the [console](./console).


</TabItem>
<TabItem value="mpy">
<details>
  <summary>Connect to any RPI RP2350 or RP2040 board (click for image)</summary>
<div style={{ textAlign: 'center'}}>
![Arduino UNO 4 Daisylinked](pathname:///img/micropython-connection.webp)
</div>
</details>

```py
import time
import machine
import duelink
 
from duelink import transport
from machine import Pin, Timer
 
sclPIN = machine.Pin(23)
sdaPIN = machine.Pin(22)
 
i2c = transport.I2CTransportController(sda=sdaPIN, scl=sclPIN)
due = duelink.DUELinkController(i2c)

# Blink the STAT LED (on for 200ms, off for 800ms, 20 times)
due.System.StatLed(200, 800, 20)
```

You need to install `duelink-stdlib-mp`. Full details are on the [MicroPython](./language/micropython) page.


</TabItem>
<TabItem value="microblocks">

This is an excellent block-coding option. It can be loaded on modules directly, or used to control tethered modules.

<details>
  <summary>Never used MicroBlocks? Click for a quick start!</summary>

Go to [microblocks.fun/run/microblocks.html](https://microblocks.fun/run/microblocks.html).

<div style={{ textAlign: 'center'}}>
![MicroBlocks](pathname:///img/microblocks-connect-1.webp)
</div>

Click `Connect` on the top right, then select `connect (USB)`. You now should have a pop-up window with `DUELink MicroBlocks` option. If you do not see that option then you may need to [load the MicroBlocks firmware](https://loader.duelink.com/microblocks).

MicroBlocks has "live updates". Click on `set user LED` and the STAT LED on your board will come on.

<div style={{ textAlign: 'center'}}>
![MicroBlocks](pathname:///img/microblocks-setuserled.webp)
</div>

Full details are on the MicroBlocks page.

<div style={{ textAlign: 'center'}}>
<Button
        style={{ margin: "10px", color: "white" }}
        label="MicroBlocks"
        link="./language/microblocks"
    />
</div>
</details>

MicroBlocks maps its blocks to work with DUELink. For example, `set user LED` is mapped to STAT LED found on all modules. Put that in a loop with some delays and we have a blinking STAT LED. This program gets stored on the device and the loop will execute automatically when the system starts, **"when started"**.

<div style={{ textAlign: 'center'}}>
![MicroBlocks](pathname:///img/mb-blink-led-loop.png)
</div>

These blocks run on the module itself. You can also use MicroBlocks to control other tethered modules. See [MicroBlocks](./language/microblocks) page for more details.

</TabItem>
</Tabs>

---

**How it works:**

1. Plug in your device.
2. Create a Project.
3. Add DUELink library.
4. Paste the above code.
5. Run the project and watch the STAT LED💡blink.


<div style={{  width: '200px'}}>
    ![StatLED](pathname:///img/statled-blink.webp)
</div>

Problems? You may need to [install the latest firmware](https://loader.duelink.com/).

<div style={{textAlign: 'center'}}>
    <Button
        style={{ margin: "10px", color: "white" }}
        label="🚀Project Showcase"
        link="./projects"
    />
</div>

---

**Choose your Tutorial:**

We have hand selected a few easy-start options, but other modules work similarly.

<div style={{textAlign: 'center'}}>
<table>
<td width='25%'>
    <img alt="ghizzy" src="/img/catalog/spghizzy-d-1.webp" />
    <Button
        style={{ margin: "10px", color: "white" }}
        label="📖Ghizzy"
        link="./tutorial/ghizzy"
    />
</td>
<td width='25%'>
    <img alt="duestem" src="/img/catalog/mcduestem-b-1.webp" />
    <Button
        style={{ margin: "10px", color: "white" }}
        label="📖DueSTEM"
        link="./tutorial/duestem"
    />
</td>
<td width='25%'>
    <img alt="cincobit" src="/img/catalog/mccincobit-e-1.webp" />
    <Button
        style={{ margin: "10px", color: "white" }}
        label="📖CincoBit"
        link="./tutorial/cincobit"
    />
</td>
<td width='25%'>
    <img alt="cincobit" src="/img/catalog/ktessen-a-1.webp" />
    <Button
        style={{ margin: "10px", color: "white" }}
        label="📖Essentials Kit"
        link="./tutorial/essentials-kit"
    />
</td>
</table>
</div>

**Try something else:**

<div style={{ textAlign: 'center'}}>
<Button style={{background:"whitesmoke", margin: "10px", color: "black" }}
        label="🎨Draw on Screen"
        link="./catalog/products?category=display"/>
<Button style={{background:"whitesmoke", margin: "10px", color: "black" }}
        label="🎶Play Sound"
        link="./catalog/products?category=sound"/>
<Button style={{background:"whitesmoke", margin: "10px", color: "black" }}
        label="🌡️Sense Everything"
        link="./catalog/products?category=sensor"/>
<Button style={{background:"whitesmoke", margin: "10px", color: "black" }}
        label="🎮Control Actuators"
        link="./catalog/products?category=actuator"/>
<Button style={{background:"whitesmoke", margin: "10px", color: "black" }}
        label="🎛️Read Inputs"
        link="./catalog/products?category=hmi"/>
<Button style={{background:"whitesmoke", margin: "10px", color: "black" }}
        label="🛜Wireless IoT"
        link="./catalog/products?category=wireless"/>
<Button style={{background:"whitesmoke", margin: "10px", color: "black" }}
        label="🧠AI Project ➚"
        link="https://forums.ghielectronics.com/t/duelink-ai-control-hardware-with-natural-language/26258"/>
<br/>
<br/>

<div style={{ textAlign: 'center'}}>
![DUELink Connection](pathname:///img/duelink-modules.webp)
</div>

No matter of you are and [Engineer](./engineers), a [Maker](./makers), or an [Educator](./educators), DUELink has somethign for you!

<Button
        style={{ margin: "10px", color: "white" }}
        label="Product Catalog"
        link="./catalog/products"
    />
</div>
---

## Modules Discovery

[loader.duelink.com/discover](https://loader.duelink.com/discover) queries connected modules. Start by USB-connecting your module(s) to a PC. If your board does not have a USB connector then use an adapter, like USB Hook.

<div style={{ textAlign: 'center'}}>
![DUELink Connection](pathname:///img/duelink-connection.webp)
</div>

Click the `Discover` button to start discovering modules! Here is an example showing a connected Ghizzy. 

<div style={{ textAlign: 'center'}}>
![CincoBit](pathname:///img/discover-ghizzy.webp)
</div>

If the "discover" feature failed complaining about a MicroBlocks firmware, then you need to [load the DUELink Official](https://loader.duelink.com/update) firmware.

:::tip 
Click `View Product Details` on the discovered item to visit the product's page.
:::

---

## Community Forum

The examples above show different ways to start with DUELink. Join us at the DUELink Forum to discuss other options and share what you are working on.

<div style={{ textAlign: 'center'}}>
<Button
        style={{ margin: "10px", color: "white" }}
        label="Forums ➚"
        link="https://forums.ghielectronics.com/c/duelink/31"
    />
</div>