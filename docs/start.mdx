---
title: Get Started
hide_table_of_contents: true
---

import Tabs from "@theme/Tabs";
import TabItem from "@theme/TabItem";

# Get Started

Every DUELink module has its own set of text commands — see each module's page in the [Product Catalog](./catalog/products) for the full list.

One command lives on **every** module: `StatLed(on, off, count)` blinks the on-board STAT LED. The three numbers are on-time, off-time, and count — `StatLed(200, 200, 5)` means on for 200ms, off for 200ms, repeated 5 times.

<div style={{ width: '200px', margin: '0 auto'}}>
    ![STAT LED blinking](pathname:///img/statled-blink.webp)
</div>

---

## Try a command in your browser (optional)

This step is optional — you can skip straight to writing code below — but it's a great way to see a module respond before any install or project setup.

Plug your DUELink module into USB. Some modules have a built-in USB connector; the rest connect through a small adapter like [USB Hook](./products/adusbhook-a). Every module supports USB once connected.

<div style={{ textAlign: 'center'}}>
![DUELink Connection](pathname:///img/duelink-connection.webp)
</div>

Visit [demo.duelink.com](https://demo.duelink.com/) and type `StatLed(200,200,5)` to blink STAT LED 5 times. Commands that read a value — like a sensor — print (return) the answer. See your specific module's available commands on the [Product Catalog](./catalog/products).

Problems? You may need to [install the latest firmware](./loader).

---

## Use it from your code

The protocol is just text over USB, I2C, or UART — you can send commands yourself from any host. To make it easier, we ship official libraries for **Python**, **JavaScript**, **.NET**, **Arduino**, and **MicroPython** that handle the transport and parse the values modules return.

Every library exposes the same `ExecuteCommand` function that sends a command and returns the result. This function can be used to send the `StatLed` command for example.

```
duelink.Engine.ExecuteCommand("StatLed(200,200,5)")
```

:::tip
Wrappers like `duelink.System.StatLed()` and other built in standard library function (commands) are provided for further simplicity. All these wrappers use `ExecuteCommand()` internally.
:::

Pick your language and plug in your device to blink that STAT LED from code.

<Tabs groupid="language"
  values={[
    {label: 'Python', value: 'py'},
    {label: 'JavaScript', value: 'js'},
    {label: '.NET', value: 'dotnet'},
    {label: 'Arduino', value: 'arduino'},
    {label: 'MicroPython', value: 'mpy'},
]}>
<TabItem value="py">
<details>
  <summary>Connect to a PC (click for image)</summary>
<div style={{ textAlign: 'center'}}>
![DUELink Connection](pathname:///img/duelink-connection.webp)
</div>
</details>

```py
from DUELink.DUELinkController import DUELinkController

availablePort = DUELinkController.GetConnectionPort()
duelink = DUELinkController(availablePort)

# Blink the STAT LED (on 200ms, off 200ms, 5 times)
duelink.Engine.ExecuteCommand("StatLed(200,200,5)")
```
You need `pip install DUELink`. Full details are on the [Python](./language/python) page.

</TabItem>
<TabItem value="js">
<details>
  <summary>Connect to a PC (click for image)</summary>
<div style={{ textAlign: 'center'}}>
![DUELink Connection](pathname:///img/duelink-connection.webp)
</div>
</details>
```js
import pkg_serialusb from 'dlserialusb';
const {SerialUSB} = pkg_serialusb

import pkg_duelink from 'duelink';
const {DUELinkController} = pkg_duelink

let duelink = new DUELinkController(new SerialUSB());
await duelink.Connect();

// Blink the STAT LED (on 200ms, off 200ms, 5 times)
await duelink.Engine.ExecuteCommand("StatLed(200,200,5)");
```
You need `npm install duelink` and `npm install dlserialusb`. Full details are on the [JavaScript](./language/javascript) page.
</TabItem>
<TabItem value="arduino">
<details>
  <summary>Connect to Arduino UNO R4 (click for image)</summary>
<div style={{ textAlign: 'center'}}>
![Arduino UNO 4 Daisylinked](pathname:///img/arduino-connection.webp)
</div>
</details>
```cpp
#include <DUELink.h>

TwoWireTransport transport(Wire1);
DUELink duelink(transport);

void setup() {
  duelink.Connect();
  // Blink the STAT LED (on 200ms, off 200ms, 5 times)
  duelink.Engine.ExecuteCommand("StatLed(200,200,5)");
}

void loop(){
}
```
You need `DUELink` and `STM32 MCU based boards` packages. Full details are on the [Arduino](./hw/arduino) page.

</TabItem>
<TabItem value="dotnet">
<details>
  <summary>Connect to a PC (click for image)</summary>
<div style={{ textAlign: 'center'}}>
![DUELink Connection](pathname:///img/duelink-connection.webp)
</div>
</details>

```cs
using GHIElectronics.DUELink;

var availablePort = DUELinkController.GetConnectionPort();
var duelink = new DUELinkController(availablePort);

// Blink the STAT LED (on 200ms, off 200ms, 5 times)
duelink.Engine.ExecuteCommand("StatLed(200,200,5)");
```
You need the `GHIElectronics.DUELink` library from `nuget.org`. Full details are on the [.NET](./language/dotnet) page.

</TabItem>

<TabItem value="mpy">
<details>
  <summary>Connect to any RPI RP2350 or RP2040 board (click for image)</summary>
<div style={{ textAlign: 'center'}}>
![MicroPython Connection](pathname:///img/micropython-connection.webp)
</div>
</details>

```py
import time
import machine
import duelink
 
from duelink import transport
from machine import Pin, Timer
 
sclPIN = machine.Pin(23)
sdaPIN = machine.Pin(22)
 
i2c = transport.I2CTransportController(sda=sdaPIN, scl=sclPIN)
due = duelink.DUELinkController(i2c)

# Blink the STAT LED (on 200ms, off 200ms, 5 times)
due.Engine.ExecuteCommand("StatLed(200,200,5)")
```

You need to install `duelink-stdlib-mp`. Full details are on the [MicroPython](./language/micropython) page.

</TabItem>
</Tabs>

---

## How it works

Every DUELink module runs its own tiny firmware that listens for **plain ASCII text** over USB, I2C, or UART. When the module recognizes a command, it runs it. When the command returns a value — like a sensor reading — the module sends the answer back as text, terminated with a carriage return.

So confirming a connection literally looks like this on the line:

```
> StatLed(200,200,5)
>
```

A command that returns a value — for example, a distance module might answer `12.4` after you send its read command — prints the answer before the prompt comes back. Those commands are **different on every module**; see that module's **Commands** tab in the [catalog](./catalog/products).

That's the entire protocol. No register maps, no SPI dance, no datasheet drill-down. The command names are the same whatever host language you write in and whatever interface you connect over — USB, I2C, or UART.

`StatLed(on, off, count)` is the one command on **every** module. Everything else is module-specific — a relay module has `RelayOn(n)`, a display has `DrawLine(...)` and `Show()`, and so on.

---

## What's connected?

Not sure which module you have plugged in? [loader.duelink.com/discover](https://loader.duelink.com/discover) queries everything on the bus and tells you what it finds. USB-connect your module(s) to a PC (use an adapter like [USB Hook](./products/adusbhook-a) if the board has no USB connector), then click **Discover**.

<div style={{ textAlign: 'center'}}>
![DUELink Connection](pathname:///img/duelink-connection.webp)
</div>

<div style={{ textAlign: 'center'}}>
![Discover output for a connected Ghizzy](pathname:///img/discover-ghizzy.webp)
</div>

:::tip
Click **View Product Details** on any discovered item to jump straight to its catalog page — including its command list.
:::

If Discover complains about a MicroBlocks firmware, [load the DUELink Official firmware](https://loader.duelink.com/update) first.

---

## Try something else

<div style={{ textAlign: 'center'}}>
<Button style={{background:"whitesmoke", margin: "10px", color: "black" }}
        label="🎨Draw on Screen"
        link="./catalog/products?category=display"/>
<Button style={{background:"whitesmoke", margin: "10px", color: "black" }}
        label="🎶Play Sound"
        link="./catalog/products?category=sound"/>
<Button style={{background:"whitesmoke", margin: "10px", color: "black" }}
        label="🌡️Sense Everything"
        link="./catalog/products?category=sensor"/>
<Button style={{background:"whitesmoke", margin: "10px", color: "black" }}
        label="🎮Control Actuators"
        link="./catalog/products?category=actuator"/>
<Button style={{background:"whitesmoke", margin: "10px", color: "black" }}
        label="🎛️Read Inputs"
        link="./catalog/products?category=hmi"/>
<Button style={{background:"whitesmoke", margin: "10px", color: "black" }}
        label="🛜Wireless IoT"
        link="./catalog/products?category=wireless"/>
<Button style={{background:"whitesmoke", margin: "10px", color: "black" }}
        label="🧠AI Project ➚"
        link="https://forums.ghielectronics.com/t/duelink-ai-control-hardware-with-natural-language/26258"/>

<br/>
<br/>

![DUELink modules](pathname:///img/duelink-modules.webp)

</div>

Whether you're prototyping commercial hardware, building a personal project, or teaching a classroom, DUELink has something for you — see [What is DUELink?](./what-is) for the big picture, or jump straight to the [Educators page](./educators) for classroom-specific guidance.

Stuck? Ask on the [DUELink forum](https://forums.ghielectronics.com/c/duelink/31).
